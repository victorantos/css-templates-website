@section PreviewCsstemplate{
    <div ng-show="showPreviewPage">
        @Html.Partial("_PreviewCsstemplate")
    </div>
}
<div ng-view> </div>

@section SideBar{
    <div ng-hide="hideSideBar">
        @Html.Partial("_SideBar")
    </div>
}

